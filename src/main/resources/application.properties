spring.application.name=Inventory-Tracking-System
server.port=8081

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.jfwwwxcqbrfrsknkihml
spring.datasource.password=Gend@r78690
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true

spring.security.user.name=admin
spring.security.user.password=admin123

# Reduce max pool size
spring.datasource.hikari.maximum-pool-size=10
# Set minimum idle connections
spring.datasource.hikari.minimum-idle=5
# Set timeout for idle connections (in milliseconds)
spring.datasource.hikari.idle-timeout=60000
# Connection timeout
spring.datasource.hikari.connection-timeout=30000
# Set leak detection threshold
spring.datasource.hikari.leak-detection-threshold=60000

spring.datasource.hikari.data-source-properties.prepareThreshold=0
